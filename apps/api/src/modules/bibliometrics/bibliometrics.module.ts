import { Module } from '@nestjs/common';

/**
 * Bibliometric indices, networks, knowledge graph and 3D mapping.
 * Scaffold stub — to be implemented per docs/ai-layer.md.
 */
@Module({})
export class BibliometricsModule {}
