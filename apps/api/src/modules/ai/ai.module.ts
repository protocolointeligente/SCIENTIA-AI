import { Module } from '@nestjs/common';

/**
 * AiOrchestratorService — multi-model routing (OpenAI/Anthropic/Google),
 * MODEL_REGISTRY/ROUTING_TABLE, ModelRun/PromptLog provenance.
 * Scaffold stub — to be implemented per docs/ai-layer.md and docs/backend-modules.md.
 */
@Module({})
export class AiModule {}
