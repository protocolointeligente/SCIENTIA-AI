import { Module } from '@nestjs/common';

/**
 * Export jobs (PDF/DOCX/XLSX/CSV/BibTeX/RIS) processed via the `exports` BullMQ queue.
 * Scaffold stub — to be implemented per docs/architecture.md.
 */
@Module({})
export class ExportsModule {}
