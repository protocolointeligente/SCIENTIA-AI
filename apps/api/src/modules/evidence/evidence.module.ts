import { Module } from '@nestjs/common';

/**
 * Evidence engine (EvidenceQuestion/EvidenceAnswer/StudyEvidenceAssessment).
 * Scaffold stub — to be implemented per docs/ai-layer.md.
 */
@Module({})
export class EvidenceModule {}
