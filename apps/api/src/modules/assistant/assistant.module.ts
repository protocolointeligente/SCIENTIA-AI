import { Module } from '@nestjs/common';

/**
 * Scientific chat assistant with RAG grounding.
 * Scaffold stub — to be implemented per docs/ai-layer.md.
 */
@Module({})
export class AssistantModule {}
