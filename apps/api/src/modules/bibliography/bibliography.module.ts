import { Module } from '@nestjs/common';

/**
 * Collections, notes, highlights and tags.
 * Scaffold stub — to be implemented per docs/data-model.md.
 */
@Module({})
export class BibliographyModule {}
