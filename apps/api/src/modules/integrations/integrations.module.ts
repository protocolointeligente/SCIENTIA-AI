import { Module } from '@nestjs/common';

/**
 * Clients for external academic sources (Crossref, Semantic Scholar,
 * PubMed, arXiv) and other third-party providers.
 * Scaffold stub — see apps/api/src/modules/search for the OpenAlex client.
 */
@Module({})
export class IntegrationsModule {}
